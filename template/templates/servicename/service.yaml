{{- if .Values.enabled }}
{{- /* Not gated on `deploymentType`: every supported kind (Deployment/StatefulSet/DaemonSet) can
sit behind a Service, and `service.enabled` is the switch for components that take no traffic.
This matches metrics.yaml, which is gated the same way. */ -}}
{{- if .Values.servicename.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "servicename") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "servicename")) | nindent 4 }}
spec:
  type: {{ .Values.servicename.service.type | default "ClusterIP" }}
  ports:
  - port: {{ .Values.servicename.service.port | default 80 }}
    targetPort: {{ .Values.servicename.containerPortName | default .Values.servicename.containerPort | default 8080 }}
    protocol: {{ .Values.servicename.service.protocol | default "TCP" }}
    {{- if and (eq .Values.servicename.service.type "NodePort") .Values.servicename.service.nodePort }}
    nodePort: {{ .Values.servicename.service.nodePort }}
    {{- end }}
    name: {{ .Values.servicename.service.portName | default "http" }}
  {{- if .Values.servicename.service.extraPorts -}}
  {{- toYaml .Values.servicename.service.extraPorts | nindent 2 -}}
  {{- end }}
  selector: {{- include "helper.selectorLabels" (dict "root" . "componentName" "servicename") | nindent 4 }}
{{- end }}
{{- end }}
