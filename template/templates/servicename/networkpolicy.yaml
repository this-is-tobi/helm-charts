{{- if .Values.enabled }}
{{- if .Values.servicename.networkPolicy.create }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "servicename") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "servicename")) | nindent 4 }}
  {{- if .Values.servicename.networkPolicy.labels }}
    {{- toYaml .Values.servicename.networkPolicy.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.servicename.networkPolicy.annotations }}
  annotations: {{- toYaml .Values.servicename.networkPolicy.annotations | nindent 4 }}
  {{- end }}
spec:
  {{- /* Required: an omitted podSelector defaults to `{}`, which would apply these rules to EVERY
  pod in the release namespace instead of just this component's pods. */}}
  podSelector:
    matchLabels: {{- include "helper.selectorLabels" (dict "root" . "componentName" "servicename") | nindent 6 }}
  policyTypes: {{- toYaml .Values.servicename.networkPolicy.policyTypes | nindent 2 }}
  {{- with .Values.servicename.networkPolicy.ingress }}
  ingress: {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.servicename.networkPolicy.egress }}
  egress: {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}
{{- end }}
