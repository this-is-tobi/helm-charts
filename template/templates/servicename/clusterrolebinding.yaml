{{- if .Values.enabled }}
{{- /* Gated on `rules` as well, exactly like clusterrole.yaml: without it, an empty `rules` list
yields a ClusterRoleBinding whose roleRef points at a ClusterRole that was never created. */ -}}
{{- if and .Values.servicename.serviceAccount.enabled .Values.servicename.serviceAccount.clusterRole.create .Values.servicename.serviceAccount.clusterRole.rules }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "servicename") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "servicename")) | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "servicename") }}
subjects:
- kind: ServiceAccount
  name: {{ .Values.servicename.serviceAccount.name | default (include "helper.componentFullname" (dict "root" . "componentName" "servicename")) }}
  namespace: {{ .Release.Namespace }}
{{- end }}
{{- end }}
