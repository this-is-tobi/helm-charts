{{- if .Values.enabled }}
{{- range $index, $value := .Values.servicename.imagePullSecrets }}
{{- if and $value.create $value.username $value.password }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "helper.imagePullSecretName" (dict "root" $ "componentName" "servicename" "index" $index "value" $value) }}
  labels: {{- (include "helper.labels" (dict "root" $ "componentName" "servicename")) | nindent 4 }}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ template "helper.imagePullSecret" dict "username" $value.username "password" $value.password "email" $value.email "registry" $value.registry }}
{{- end }}
{{- end }}
{{- end }}
