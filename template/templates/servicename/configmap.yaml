{{- if .Values.enabled }}
{{- if or .Values.servicename.envCm .Values.global.envCm -}}
kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "servicename") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "servicename")) | nindent 4 }}
data:
  {{- if .Values.global.envCm -}}
  {{- include "helper.env" (dict "root" . "data" .Values.global.envCm) | indent 2 }}
  {{- end -}}
  {{- if .Values.servicename.envCm -}}
  {{- include "helper.env" (dict "root" . "data" .Values.servicename.envCm) | indent 2 }}
  {{- end -}}
{{- end -}}
{{- end }}
