{{- if .Values.servicename.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%s-%s" (include "helper.fullname" .) "servicename" "metrics" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "servicename")) | nindent 4 }}
  {{- if .Values.servicename.metrics.service.labels }}
    {{- toYaml .Values.servicename.metrics.service.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.servicename.metrics.service.annotations }}
  annotations: {{- toYaml .Values.servicename.metrics.service.annotations | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.servicename.metrics.service.type }}
  ports:
  - port: {{ .Values.servicename.metrics.service.port }}
    targetPort: {{ .Values.servicename.metrics.service.targetPort }}
    protocol: TCP
  selector: {{- include "helper.selectorLabels" . | nindent 4 }}
{{- end }}
