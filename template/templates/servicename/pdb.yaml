{{- if .Values.enabled }}
{{- if .Values.servicename.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "servicename") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "servicename")) | nindent 4 }}
  {{- if .Values.servicename.pdb.labels }}
    {{- toYaml .Values.servicename.pdb.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.servicename.pdb.annotations }}
  annotations: {{- toYaml .Values.servicename.pdb.annotations | nindent 4 }}
  {{- end }}
spec:
  {{- /* Tested against the empty string rather than for truthiness: `maxUnavailable: 0` is a valid
  setting (it blocks every voluntary eviction) but is falsy, so `with`/`if` would silently fall
  through to minAvailable. `toString` maps both nil and "" to "", which are the two unset forms.
  `helper.component.validate` guarantees at least one of the two is set. */ -}}
  {{- if ne (toString .Values.servicename.pdb.maxUnavailable) "" }}
  maxUnavailable: {{ .Values.servicename.pdb.maxUnavailable }}
  {{- else }}
  minAvailable: {{ .Values.servicename.pdb.minAvailable }}
  {{- end }}
  selector:
    matchLabels: {{- include "helper.selectorLabels" (dict "root" . "componentName" "servicename") | nindent 6 }}
{{- end }}
{{- end }}
